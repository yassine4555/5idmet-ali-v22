# EDUPRO Backend environment example
# Copy to .env and set real secrets before running in production
PORT=3001
MONGODB_URI=mongodb://localhost:27017/edupro
JWT_SECRET=replace_this_with_a_strong_random_secret
# When true, allows the legacy demo password 'demo123' for local/dev testing only
ALLOW_DEMO_LOGIN=false
